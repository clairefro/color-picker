import React from 'react';

const ColorPaletteContainer = ({ hue }) => (
  <div className="color-palette-container">
    <div className="test" >
      <p>{hue}</p>
    </div>
  </div>
);

export default ColorPaletteContainer;
